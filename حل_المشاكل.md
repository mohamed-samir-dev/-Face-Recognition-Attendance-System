# 🔧 دليل حل المشاكل - IntelliAttend

## 🚨 المشاكل الشائعة وحلولها

---

## 1️⃣ مشاكل Backend

### ❌ خطأ: "ModuleNotFoundError: No module named 'flask'"
**الحل:**
```bash
cd backend
venv\Scripts\activate
pip install -r requirements/requirements.txt
```

### ❌ خطأ: "No module named 'face_recognition'"
**الحل:**
```bash
pip install cmake
pip install dlib==19.24.2
pip install face-recognition==1.3.0
```

### ❌ خطأ: "Port 5001 is already in use"
**الحل:**
```bash
# أوقف العملية التي تستخدم المنفذ
netstat -ano | findstr :5001
taskkill /PID [رقم_العملية] /F

# أو غيّر المنفذ في ملف config
```

### ❌ خطأ: "Firebase credentials not found"
**الحل:**
- تأكد من وجود ملف `user-login-data-*.json` في المجلد الرئيسي
- تحقق من المسار في ملف `firebase_service.py`

---

## 2️⃣ مشاكل Frontend

### ❌ خطأ: "npm ERR! code ENOENT"
**الحل:**
```bash
cd frontend
npm install
```

### ❌ خطأ: "Port 3000 is already in use"
**الحل:**
```bash
# أوقف العملية
netstat -ano | findstr :3000
taskkill /PID [رقم_العملية] /F

# أو شغّل على منفذ آخر
npm run dev -- -p 3001
```

### ❌ الصفحة بيضاء فارغة
**الحل:**
```bash
# احذف .next وأعد البناء
rmdir /s /q .next
npm run dev
```

### ❌ خطأ: "Failed to fetch"
**الحل:**
- تأكد من تشغيل Backend على `http://localhost:5001`
- تحقق من إعدادات CORS
- افحص Console في المتصفح للمزيد من التفاصيل

---

## 3️⃣ مشاكل الكاميرا

### ❌ "Camera access denied"
**الحل:**
1. اذهب لإعدادات المتصفح
2. ابحث عن "Camera permissions"
3. اسمح لـ `localhost:3000` بالوصول للكاميرا

**في Chrome:**
- Settings → Privacy and security → Site settings → Camera
- أضف `http://localhost:3000` للمواقع المسموحة

### ❌ الكاميرا سوداء أو لا تعمل
**الحل:**
- أعد تشغيل المتصفح
- تأكد من عدم استخدام برنامج آخر للكاميرا (Zoom, Teams, etc.)
- جرب متصفح آخر (Chrome مفضل)
- تحقق من تعريفات الكاميرا في Device Manager

### ❌ "getUserMedia is not supported"
**الحل:**
- استخدم HTTPS أو localhost فقط
- حدّث المتصفح لآخر إصدار
- استخدم Chrome أو Edge (Firefox قد يواجه مشاكل)

---

## 4️⃣ مشاكل التعرف على الوجه

### ❌ "No face detected"
**الحل:**
- تأكد من وجود إضاءة جيدة
- اقترب أكثر من الكاميرا
- انظر مباشرة للكاميرا
- أزل النظارات الشمسية أو الكمامة

### ❌ "Face recognition failed"
**الحل:**
- تأكد من تسجيل صورتك في النظام
- جرب التقاط صورة أوضح
- تحقق من جودة الكاميرا
- تأكد من تدريب النموذج على صورتك

### ❌ "Multiple faces detected"
**الحل:**
- تأكد من وجود وجه واحد فقط في الكادر
- اطلب من الآخرين الابتعاد عن الكاميرا

---

## 5️⃣ مشاكل Firebase

### ❌ "Firebase initialization failed"
**الحل:**
```bash
# تأكد من تثبيت Firebase
pip install firebase-admin==6.2.0

# تحقق من ملف credentials
# يجب أن يكون في المجلد الرئيسي
```

### ❌ "Permission denied" في Firebase
**الحل:**
- تحقق من صلاحيات Firebase في Console
- تأكد من تفعيل Firestore Database
- راجع قواعد الأمان (Security Rules)

### ❌ "Network error" عند الاتصال بـ Firebase
**الحل:**
- تحقق من اتصال الإنترنت
- تأكد من عدم حظر Firewall لـ Firebase
- جرب VPN إذا كان هناك حظر إقليمي

---

## 6️⃣ مشاكل الأداء

### ❌ النظام بطيء جداً
**الحل:**
```bash
# Backend: استخدم caching
# Frontend: امسح cache المتصفح

# في Chrome:
# Ctrl + Shift + Delete → Clear cache
```

### ❌ استهلاك عالي للذاكرة
**الحل:**
- أغلق التطبيقات الأخرى
- أعد تشغيل Backend و Frontend
- تحقق من Task Manager لمعرفة العملية المستهلكة

---

## 7️⃣ مشاكل تسجيل الدخول

### ❌ "Invalid credentials"
**الحل:**
- تأكد من كتابة Username و Password بشكل صحيح
- جرب الحسابات الافتراضية:
  - admin / admin123
  - supervisor / super123
  - employee / emp123

### ❌ "User not found"
**الحل:**
- تحقق من وجود المستخدم في Firebase
- تأكد من إضافة المستخدم بشكل صحيح
- راجع collection "users" في Firestore

---

## 8️⃣ مشاكل إضافة موظف جديد

### ❌ "Failed to add employee"
**الحل:**
- تأكد من رفع صورة واضحة للوجه
- تحقق من حجم الصورة (يجب أن تكون < 5MB)
- تأكد من أن الصورة تحتوي على وجه واحد فقط

### ❌ "Email sending failed"
**الحل:**
- تحقق من إعدادات Email في `.env.local`
- تأكد من صحة بيانات SMTP
- راجع console للمزيد من التفاصيل

---

## 9️⃣ مشاكل عامة

### ❌ "CORS policy error"
**الحل:**
```python
# في Backend: تأكد من تفعيل CORS
from flask_cors import CORS
CORS(app, resources={r"/*": {"origins": "*"}})
```

### ❌ الصفحة لا تتحدث مع Backend
**الحل:**
1. تأكد من تشغيل Backend على `http://localhost:5001`
2. تأكد من تشغيل Frontend على `http://localhost:3000`
3. افحص Network tab في Developer Tools
4. تحقق من عدم وجود Firewall يحجب الاتصال

---

## 🔍 أدوات التشخيص

### فحص Backend:
```bash
# اختبر إذا Backend يعمل
curl http://localhost:5001/

# أو افتح في المتصفح
http://localhost:5001/
```

### فحص Frontend:
```bash
# افتح Developer Tools في المتصفح
# F12 أو Ctrl + Shift + I

# راجع:
# - Console: للأخطاء JavaScript
# - Network: لطلبات API
# - Application: لـ sessionStorage
```

### فحص Firebase:
```bash
# في Python console:
python
>>> from firebase_admin import credentials, firestore, initialize_app
>>> cred = credentials.Certificate('user-login-data-*.json')
>>> initialize_app(cred)
>>> db = firestore.client()
>>> print(db.collection('users').get())
```

---

## 📞 الحصول على مساعدة إضافية

إذا لم تحل المشكلة:

1. **راجع Logs:**
   - Backend: رسائل في Terminal
   - Frontend: Console في المتصفح

2. **ابحث عن الخطأ:**
   - انسخ رسالة الخطأ الكاملة
   - ابحث عنها في Google

3. **راجع الوثائق:**
   - `دليل_التشغيل.md`
   - `PROJECT_EXPLANATION_AR.md`
   - `README.md`

4. **أعد التثبيت:**
   ```bash
   # Backend
   cd backend
   rmdir /s /q venv
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements/requirements.txt
   
   # Frontend
   cd frontend
   rmdir /s /q node_modules
   npm install
   ```

---

## ✅ قائمة التحقق النهائية

قبل طلب المساعدة، تأكد من:

- [ ] Python 3.8+ مثبت بشكل صحيح
- [ ] Node.js 18+ مثبت بشكل صحيح
- [ ] جميع المكتبات مثبتة (pip install & npm install)
- [ ] ملف Firebase موجود في المكان الصحيح
- [ ] Backend يعمل على port 5001
- [ ] Frontend يعمل على port 3000
- [ ] الكاميرا تعمل ومسموح لها بالوصول
- [ ] اتصال الإنترنت نشط
- [ ] لا يوجد Firewall يحجب الاتصالات

---

**💡 نصيحة:** احتفظ بهذا الملف مفتوحاً أثناء التطوير للرجوع إليه عند الحاجة!

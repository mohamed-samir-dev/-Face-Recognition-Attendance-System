# 🚀 دليل تشغيل موقع IntelliAttend الكامل

## 📋 المتطلبات الأساسية

### 1. البرامج المطلوبة:
- **Python 3.8+** (للـ Backend)
- **Node.js 18+** (للـ Frontend)
- **npm** أو **yarn** (مدير الحزم)
- **Git** (اختياري)
- **كاميرا ويب** (للتعرف على الوجه)

---

## 🔧 خطوات التشغيل الكاملة

### المرحلة 1️⃣: إعداد Backend (Python/Flask)

#### الخطوة 1: فتح Terminal في مجلد Backend
```bash
cd "b:\مشروع التخرج\face-recognition-attendance-system\backend"
```

#### الخطوة 2: إنشاء بيئة افتراضية (Virtual Environment)
```bash
# إنشاء البيئة الافتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
# في Windows:
venv\Scripts\activate

# في Mac/Linux:
source venv/bin/activate
```

#### الخطوة 3: تثبيت المكتبات المطلوبة
```bash
# تثبيت جميع المكتبات الأساسية
pip install -r requirements/requirements.txt

# تثبيت مكتبات التعرف على الوجه
pip install -r requirements/requirements_face.txt

# تثبيت مكتبات Firebase
pip install -r requirements/requirements_firebase.txt
```

**ملاحظة مهمة:** إذا واجهت مشكلة في تثبيت `dlib`، قم بتثبيته بهذه الطريقة:
```bash
# تثبيت CMake أولاً
pip install cmake

# ثم تثبيت dlib
pip install dlib==19.24.2
```

#### الخطوة 4: التحقق من ملف Firebase
تأكد من وجود ملف:
```
user-login-data-7d185-firebase-adminsdk-fbsvc-3c8a31d30f.json
```
في المجلد الرئيسي للمشروع.

#### الخطوة 5: تشغيل Backend Server
```bash
# تشغيل السيرفر المحسّن
python enhanced_face_api_server.py
```

**يجب أن ترى:**
```
Starting Enhanced Face Recognition Server on port 5001...
Encoding cache initialized
 * Running on http://127.0.0.1:5001
```

✅ **Backend جاهز الآن على:** `http://localhost:5001`

---

### المرحلة 2️⃣: إعداد Frontend (Next.js/React)

#### الخطوة 1: فتح Terminal جديد في مجلد Frontend
```bash
cd "b:\مشروع التخرج\face-recognition-attendance-system\frontend"
```

#### الخطوة 2: تثبيت المكتبات
```bash
# تثبيت جميع المكتبات المطلوبة
npm install

# أو إذا كنت تستخدم yarn:
yarn install
```

#### الخطوة 3: التحقق من ملف البيئة
تأكد من وجود ملف `.env.local` يحتوي على:
```env
EMAIL_USER=loutfallaasfora@gmail.com
EMAIL_PASS=lmntcrqvyzfnrfpj
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

#### الخطوة 4: تشغيل Frontend Server
```bash
# تشغيل في وضع التطوير
npm run dev

# أو:
yarn dev
```

**يجب أن ترى:**
```
  ▲ Next.js 15.5.4
  - Local:        http://localhost:3000
  - Ready in 2.5s
```

✅ **Frontend جاهز الآن على:** `http://localhost:3000`

---

## 🎯 التحقق من التشغيل الصحيح

### 1. اختبار Backend:
افتح المتصفح واذهب إلى:
```
http://localhost:5001/
```
يجب أن ترى رسالة: "Face Recognition API is running"

### 2. اختبار Frontend:
افتح المتصفح واذهب إلى:
```
http://localhost:3000
```
يجب أن يحولك تلقائياً لصفحة تسجيل الدخول.

---

## 👤 بيانات الدخول الافتراضية

### Admin (مدير النظام):
```
Username: admin
Password: admin123
```

### Supervisor (مشرف):
```
Username: supervisor
Password: super123
```

### Employee (موظف):
```
Username: employee
Password: emp123
```

---

## 📱 كيفية استخدام النظام

### 1. تسجيل الدخول:
1. افتح `http://localhost:3000`
2. أدخل اسم المستخدم وكلمة المرور
3. اضغط "Login"

### 2. تسجيل الحضور (للموظف):
1. بعد تسجيل الدخول، اضغط "Mark Attendance"
2. اسمح للمتصفح بالوصول للكاميرا
3. اضغط "Capture" لالتقاط صورتك
4. انتظر المصادقة الثلاثية:
   - ✅ التعرف على الوجه
   - ✅ التحقق من الاسم
   - ✅ التحقق من الرقم الوظيفي
5. سيتم تسجيل حضورك تلقائياً

### 3. إضافة موظف جديد (للـ Admin):
1. سجل دخول كـ Admin
2. اذهب إلى "Add Employee"
3. املأ البيانات:
   - الاسم
   - البريد الإلكتروني
   - القسم
   - المسمى الوظيفي
   - ارفع صورة واضحة للوجه
4. اضغط "Submit"
5. سيتم إنشاء حساب تلقائياً وإرسال بيانات الدخول

---

## 🔧 حل المشاكل الشائعة

### مشكلة 1: Backend لا يعمل
```bash
# تأكد من تفعيل البيئة الافتراضية
venv\Scripts\activate

# أعد تثبيت المكتبات
pip install -r requirements/requirements.txt
pip install -r requirements/requirements_face.txt
pip install -r requirements/requirements_firebase.txt
```

### مشكلة 2: Frontend لا يعمل
```bash
# احذف مجلد node_modules وأعد التثبيت
rmdir /s /q node_modules
npm install

# أو احذف .next وأعد البناء
rmdir /s /q .next
npm run dev
```

### مشكلة 3: الكاميرا لا تعمل
- تأكد من السماح للمتصفح بالوصول للكاميرا
- استخدم HTTPS أو localhost فقط
- جرب متصفح آخر (Chrome مفضل)

### مشكلة 4: خطأ في Firebase
- تأكد من وجود ملف Firebase JSON في المكان الصحيح
- تحقق من صلاحيات الملف
- تأكد من اتصالك بالإنترنت

### مشكلة 5: خطأ CORS
تأكد من أن Backend يعمل على `http://localhost:5001`
وأن Frontend يعمل على `http://localhost:3000`

---

## 📊 هيكل المشروع

```
face-recognition-attendance-system/
│
├── backend/                          # Backend (Python/Flask)
│   ├── app/
│   │   ├── routes/                   # API Endpoints
│   │   ├── services/                 # Business Logic
│   │   └── config/                   # Configuration
│   ├── requirements/                 # Python Dependencies
│   └── enhanced_face_api_server.py   # Main Server File
│
├── frontend/                         # Frontend (Next.js/React)
│   ├── src/
│   │   ├── app/                      # Pages & Routes
│   │   ├── components/               # React Components
│   │   └── lib/                      # Services & Utilities
│   ├── package.json                  # Node Dependencies
│   └── .env.local                    # Environment Variables
│
├── ai/                               # AI Models
│   └── firebase_face_model.py        # Face Recognition Model
│
└── user-login-data-*.json            # Firebase Credentials
```

---

## 🌐 المنافذ المستخدمة

| الخدمة | المنفذ | الرابط |
|--------|--------|--------|
| Frontend | 3000 | http://localhost:3000 |
| Backend API | 5001 | http://localhost:5001 |

---

## ⚡ أوامر سريعة

### تشغيل Backend:
```bash
cd backend
venv\Scripts\activate
python enhanced_face_api_server.py
```

### تشغيل Frontend:
```bash
cd frontend
npm run dev
```

### إيقاف السيرفرات:
اضغط `Ctrl + C` في كل Terminal

---

## 📝 ملاحظات مهمة

1. **يجب تشغيل Backend أولاً** قبل Frontend
2. **لا تغلق نوافذ Terminal** أثناء استخدام النظام
3. **استخدم كاميرا جيدة** للحصول على أفضل نتائج تعرف
4. **الإضاءة مهمة** - تأكد من وجود إضاءة جيدة عند التقاط الصورة
5. **اتصال الإنترنت مطلوب** لـ Firebase

---

## 🎓 الخطوات التالية

بعد تشغيل النظام بنجاح:

1. ✅ جرب تسجيل الدخول بالحسابات الافتراضية
2. ✅ أضف موظف جديد من لوحة Admin
3. ✅ جرب تسجيل الحضور بالتعرف على الوجه
4. ✅ استكشف لوحات التحكم المختلفة
5. ✅ راجع التقارير والإحصائيات

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. راجع قسم "حل المشاكل الشائعة" أعلاه
2. تحقق من رسائل الخطأ في Terminal
3. تأكد من تثبيت جميع المتطلبات بشكل صحيح
4. راجع ملف `PROJECT_EXPLANATION_AR.md` للتفاصيل التقنية

---

**🎉 مبروك! نظام IntelliAttend جاهز للاستخدام**
